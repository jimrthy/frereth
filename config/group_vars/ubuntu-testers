# -*- mode: yaml -*-
---

# I have more than a sneaking suspicion that this list is duplicated
# in wherever I planned to use it.
build_packages:
  - g++
  - libtool
  - autoconf
  - gettext
  - make
  # This next one is freaking huge!
  # (Well, tons of dependencies)
  - maven
  - pkg-config
