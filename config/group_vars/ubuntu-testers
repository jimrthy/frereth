# -*- mode: yaml -*-
---
lein_dir: /usr/local/bin/lein
system_packages:
  # Would be using this if I were building a server
  # - openjdk-7-jre-headless
  - openjdk-7-jdk
  - vim
  - emacs
  - tmux
  - zsh
  - python-pip
  - git
  - subversion
  - ssh
  # Included w/ ssh
  # which means it'll be very useful for hosts where I just
  # want to install something minimalist for the sake of time
  # - wget
  # e.g.
  # - openssh-server
frereth_repositories:
  - clglui
  # FIXME: Uncomment the rest (and get rid of that) when the
  # basic idea is working
  - jzmq
  - cljeromq
  - frereth
  - frereth-client
  - frereth-server
  - frereth-terminal
  - frereth-renderer
  - penumbra
  - libsodium
build_packages:
  - g++
  - libtool
  - autoconf
  - gettext
  